# build output
target/
dist/
bin/
_build/
deps/
node_modules/
*.beam
web/src/sim-wasm/

# terraform: local state is deliberate (see infra/terraform/00-cluster/main.tf),
# but it must never be committed. Lock files are the opposite — they pin
# provider versions, so they belong in the repo.
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars

# local env
.env
.env.local
.direnv/

# editors / os
.DS_Store
.idea/
.vscode/
*.swp
