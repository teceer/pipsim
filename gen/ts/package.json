{
  "name": "@pipsim/gen",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "Hand-written, not generated: buf emits .ts files but no package metadata. Mirrors gen/go/go.mod — the generated bindings are a package consumers depend on explicitly.",
  "dependencies": {
    "@bufbuild/protobuf": "^2.13.0"
  }
}
