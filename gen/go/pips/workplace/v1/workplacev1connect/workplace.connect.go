// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: pips/workplace/v1/workplace.proto

package workplacev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/teceer/pipsim/gen/go/pips/workplace/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// WorkplaceServiceName is the fully-qualified name of the WorkplaceService service.
	WorkplaceServiceName = "pips.workplace.v1.WorkplaceService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// WorkplaceServiceListProcedure is the fully-qualified name of the WorkplaceService's List RPC.
	WorkplaceServiceListProcedure = "/pips.workplace.v1.WorkplaceService/List"
	// WorkplaceServiceDescribeProcedure is the fully-qualified name of the WorkplaceService's Describe
	// RPC.
	WorkplaceServiceDescribeProcedure = "/pips.workplace.v1.WorkplaceService/Describe"
	// WorkplaceServiceCanEmployProcedure is the fully-qualified name of the WorkplaceService's
	// CanEmploy RPC.
	WorkplaceServiceCanEmployProcedure = "/pips.workplace.v1.WorkplaceService/CanEmploy"
	// WorkplaceServiceStartShiftProcedure is the fully-qualified name of the WorkplaceService's
	// StartShift RPC.
	WorkplaceServiceStartShiftProcedure = "/pips.workplace.v1.WorkplaceService/StartShift"
	// WorkplaceServiceWorkProcedure is the fully-qualified name of the WorkplaceService's Work RPC.
	WorkplaceServiceWorkProcedure = "/pips.workplace.v1.WorkplaceService/Work"
	// WorkplaceServiceEndShiftProcedure is the fully-qualified name of the WorkplaceService's EndShift
	// RPC.
	WorkplaceServiceEndShiftProcedure = "/pips.workplace.v1.WorkplaceService/EndShift"
	// WorkplaceServiceBuyProcedure is the fully-qualified name of the WorkplaceService's Buy RPC.
	WorkplaceServiceBuyProcedure = "/pips.workplace.v1.WorkplaceService/Buy"
)

// WorkplaceServiceClient is a client for the pips.workplace.v1.WorkplaceService service.
type WorkplaceServiceClient interface {
	// Every building this service hosts.
	//
	// A service owns a *kind* of building, not one building, so "who are you" has
	// no answer and `Describe` with no id has nothing to return. The gateway
	// enumerates here and then addresses each instance by id. A service hosting a
	// single building answers with one entry, which is why this is additive
	// rather than a replacement.
	List(context.Context, *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error)
	// Static description of one building. `workplace_id` is required as soon as a
	// service hosts more than one.
	Describe(context.Context, *connect.Request[v1.DescribeRequest]) (*connect.Response[v1.DescribeResponse], error)
	// Can this pip work here right now? Called by the gateway before hiring.
	CanEmploy(context.Context, *connect.Request[v1.CanEmployRequest]) (*connect.Response[v1.CanEmployResponse], error)
	// A pip starts a work shift.
	StartShift(context.Context, *connect.Request[v1.StartShiftRequest]) (*connect.Response[v1.StartShiftResponse], error)
	// A tick of work performed by an employed pip. Returns what was produced
	// and how the pip's needs changed.
	Work(context.Context, *connect.Request[v1.WorkRequest]) (*connect.Response[v1.WorkResponse], error)
	// A pip ends the shift, voluntarily or not.
	EndShift(context.Context, *connect.Request[v1.EndShiftRequest]) (*connect.Response[v1.EndShiftResponse], error)
	// A pip buys one unit of something this workplace sells. The workplace only
	// confirms the kind and the price it advertises in `Describe.sells` — it
	// never moves money itself. The gateway does that through the bank, then
	// tells sim-core what the resource does to the pip via a TransferIntent.
	Buy(context.Context, *connect.Request[v1.BuyRequest]) (*connect.Response[v1.BuyResponse], error)
}

// NewWorkplaceServiceClient constructs a client for the pips.workplace.v1.WorkplaceService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewWorkplaceServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) WorkplaceServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	workplaceServiceMethods := v1.File_pips_workplace_v1_workplace_proto.Services().ByName("WorkplaceService").Methods()
	return &workplaceServiceClient{
		list: connect.NewClient[v1.ListRequest, v1.ListResponse](
			httpClient,
			baseURL+WorkplaceServiceListProcedure,
			connect.WithSchema(workplaceServiceMethods.ByName("List")),
			connect.WithClientOptions(opts...),
		),
		describe: connect.NewClient[v1.DescribeRequest, v1.DescribeResponse](
			httpClient,
			baseURL+WorkplaceServiceDescribeProcedure,
			connect.WithSchema(workplaceServiceMethods.ByName("Describe")),
			connect.WithClientOptions(opts...),
		),
		canEmploy: connect.NewClient[v1.CanEmployRequest, v1.CanEmployResponse](
			httpClient,
			baseURL+WorkplaceServiceCanEmployProcedure,
			connect.WithSchema(workplaceServiceMethods.ByName("CanEmploy")),
			connect.WithClientOptions(opts...),
		),
		startShift: connect.NewClient[v1.StartShiftRequest, v1.StartShiftResponse](
			httpClient,
			baseURL+WorkplaceServiceStartShiftProcedure,
			connect.WithSchema(workplaceServiceMethods.ByName("StartShift")),
			connect.WithClientOptions(opts...),
		),
		work: connect.NewClient[v1.WorkRequest, v1.WorkResponse](
			httpClient,
			baseURL+WorkplaceServiceWorkProcedure,
			connect.WithSchema(workplaceServiceMethods.ByName("Work")),
			connect.WithClientOptions(opts...),
		),
		endShift: connect.NewClient[v1.EndShiftRequest, v1.EndShiftResponse](
			httpClient,
			baseURL+WorkplaceServiceEndShiftProcedure,
			connect.WithSchema(workplaceServiceMethods.ByName("EndShift")),
			connect.WithClientOptions(opts...),
		),
		buy: connect.NewClient[v1.BuyRequest, v1.BuyResponse](
			httpClient,
			baseURL+WorkplaceServiceBuyProcedure,
			connect.WithSchema(workplaceServiceMethods.ByName("Buy")),
			connect.WithClientOptions(opts...),
		),
	}
}

// workplaceServiceClient implements WorkplaceServiceClient.
type workplaceServiceClient struct {
	list       *connect.Client[v1.ListRequest, v1.ListResponse]
	describe   *connect.Client[v1.DescribeRequest, v1.DescribeResponse]
	canEmploy  *connect.Client[v1.CanEmployRequest, v1.CanEmployResponse]
	startShift *connect.Client[v1.StartShiftRequest, v1.StartShiftResponse]
	work       *connect.Client[v1.WorkRequest, v1.WorkResponse]
	endShift   *connect.Client[v1.EndShiftRequest, v1.EndShiftResponse]
	buy        *connect.Client[v1.BuyRequest, v1.BuyResponse]
}

// List calls pips.workplace.v1.WorkplaceService.List.
func (c *workplaceServiceClient) List(ctx context.Context, req *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error) {
	return c.list.CallUnary(ctx, req)
}

// Describe calls pips.workplace.v1.WorkplaceService.Describe.
func (c *workplaceServiceClient) Describe(ctx context.Context, req *connect.Request[v1.DescribeRequest]) (*connect.Response[v1.DescribeResponse], error) {
	return c.describe.CallUnary(ctx, req)
}

// CanEmploy calls pips.workplace.v1.WorkplaceService.CanEmploy.
func (c *workplaceServiceClient) CanEmploy(ctx context.Context, req *connect.Request[v1.CanEmployRequest]) (*connect.Response[v1.CanEmployResponse], error) {
	return c.canEmploy.CallUnary(ctx, req)
}

// StartShift calls pips.workplace.v1.WorkplaceService.StartShift.
func (c *workplaceServiceClient) StartShift(ctx context.Context, req *connect.Request[v1.StartShiftRequest]) (*connect.Response[v1.StartShiftResponse], error) {
	return c.startShift.CallUnary(ctx, req)
}

// Work calls pips.workplace.v1.WorkplaceService.Work.
func (c *workplaceServiceClient) Work(ctx context.Context, req *connect.Request[v1.WorkRequest]) (*connect.Response[v1.WorkResponse], error) {
	return c.work.CallUnary(ctx, req)
}

// EndShift calls pips.workplace.v1.WorkplaceService.EndShift.
func (c *workplaceServiceClient) EndShift(ctx context.Context, req *connect.Request[v1.EndShiftRequest]) (*connect.Response[v1.EndShiftResponse], error) {
	return c.endShift.CallUnary(ctx, req)
}

// Buy calls pips.workplace.v1.WorkplaceService.Buy.
func (c *workplaceServiceClient) Buy(ctx context.Context, req *connect.Request[v1.BuyRequest]) (*connect.Response[v1.BuyResponse], error) {
	return c.buy.CallUnary(ctx, req)
}

// WorkplaceServiceHandler is an implementation of the pips.workplace.v1.WorkplaceService service.
type WorkplaceServiceHandler interface {
	// Every building this service hosts.
	//
	// A service owns a *kind* of building, not one building, so "who are you" has
	// no answer and `Describe` with no id has nothing to return. The gateway
	// enumerates here and then addresses each instance by id. A service hosting a
	// single building answers with one entry, which is why this is additive
	// rather than a replacement.
	List(context.Context, *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error)
	// Static description of one building. `workplace_id` is required as soon as a
	// service hosts more than one.
	Describe(context.Context, *connect.Request[v1.DescribeRequest]) (*connect.Response[v1.DescribeResponse], error)
	// Can this pip work here right now? Called by the gateway before hiring.
	CanEmploy(context.Context, *connect.Request[v1.CanEmployRequest]) (*connect.Response[v1.CanEmployResponse], error)
	// A pip starts a work shift.
	StartShift(context.Context, *connect.Request[v1.StartShiftRequest]) (*connect.Response[v1.StartShiftResponse], error)
	// A tick of work performed by an employed pip. Returns what was produced
	// and how the pip's needs changed.
	Work(context.Context, *connect.Request[v1.WorkRequest]) (*connect.Response[v1.WorkResponse], error)
	// A pip ends the shift, voluntarily or not.
	EndShift(context.Context, *connect.Request[v1.EndShiftRequest]) (*connect.Response[v1.EndShiftResponse], error)
	// A pip buys one unit of something this workplace sells. The workplace only
	// confirms the kind and the price it advertises in `Describe.sells` — it
	// never moves money itself. The gateway does that through the bank, then
	// tells sim-core what the resource does to the pip via a TransferIntent.
	Buy(context.Context, *connect.Request[v1.BuyRequest]) (*connect.Response[v1.BuyResponse], error)
}

// NewWorkplaceServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewWorkplaceServiceHandler(svc WorkplaceServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	workplaceServiceMethods := v1.File_pips_workplace_v1_workplace_proto.Services().ByName("WorkplaceService").Methods()
	workplaceServiceListHandler := connect.NewUnaryHandler(
		WorkplaceServiceListProcedure,
		svc.List,
		connect.WithSchema(workplaceServiceMethods.ByName("List")),
		connect.WithHandlerOptions(opts...),
	)
	workplaceServiceDescribeHandler := connect.NewUnaryHandler(
		WorkplaceServiceDescribeProcedure,
		svc.Describe,
		connect.WithSchema(workplaceServiceMethods.ByName("Describe")),
		connect.WithHandlerOptions(opts...),
	)
	workplaceServiceCanEmployHandler := connect.NewUnaryHandler(
		WorkplaceServiceCanEmployProcedure,
		svc.CanEmploy,
		connect.WithSchema(workplaceServiceMethods.ByName("CanEmploy")),
		connect.WithHandlerOptions(opts...),
	)
	workplaceServiceStartShiftHandler := connect.NewUnaryHandler(
		WorkplaceServiceStartShiftProcedure,
		svc.StartShift,
		connect.WithSchema(workplaceServiceMethods.ByName("StartShift")),
		connect.WithHandlerOptions(opts...),
	)
	workplaceServiceWorkHandler := connect.NewUnaryHandler(
		WorkplaceServiceWorkProcedure,
		svc.Work,
		connect.WithSchema(workplaceServiceMethods.ByName("Work")),
		connect.WithHandlerOptions(opts...),
	)
	workplaceServiceEndShiftHandler := connect.NewUnaryHandler(
		WorkplaceServiceEndShiftProcedure,
		svc.EndShift,
		connect.WithSchema(workplaceServiceMethods.ByName("EndShift")),
		connect.WithHandlerOptions(opts...),
	)
	workplaceServiceBuyHandler := connect.NewUnaryHandler(
		WorkplaceServiceBuyProcedure,
		svc.Buy,
		connect.WithSchema(workplaceServiceMethods.ByName("Buy")),
		connect.WithHandlerOptions(opts...),
	)
	return "/pips.workplace.v1.WorkplaceService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case WorkplaceServiceListProcedure:
			workplaceServiceListHandler.ServeHTTP(w, r)
		case WorkplaceServiceDescribeProcedure:
			workplaceServiceDescribeHandler.ServeHTTP(w, r)
		case WorkplaceServiceCanEmployProcedure:
			workplaceServiceCanEmployHandler.ServeHTTP(w, r)
		case WorkplaceServiceStartShiftProcedure:
			workplaceServiceStartShiftHandler.ServeHTTP(w, r)
		case WorkplaceServiceWorkProcedure:
			workplaceServiceWorkHandler.ServeHTTP(w, r)
		case WorkplaceServiceEndShiftProcedure:
			workplaceServiceEndShiftHandler.ServeHTTP(w, r)
		case WorkplaceServiceBuyProcedure:
			workplaceServiceBuyHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedWorkplaceServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedWorkplaceServiceHandler struct{}

func (UnimplementedWorkplaceServiceHandler) List(context.Context, *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.workplace.v1.WorkplaceService.List is not implemented"))
}

func (UnimplementedWorkplaceServiceHandler) Describe(context.Context, *connect.Request[v1.DescribeRequest]) (*connect.Response[v1.DescribeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.workplace.v1.WorkplaceService.Describe is not implemented"))
}

func (UnimplementedWorkplaceServiceHandler) CanEmploy(context.Context, *connect.Request[v1.CanEmployRequest]) (*connect.Response[v1.CanEmployResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.workplace.v1.WorkplaceService.CanEmploy is not implemented"))
}

func (UnimplementedWorkplaceServiceHandler) StartShift(context.Context, *connect.Request[v1.StartShiftRequest]) (*connect.Response[v1.StartShiftResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.workplace.v1.WorkplaceService.StartShift is not implemented"))
}

func (UnimplementedWorkplaceServiceHandler) Work(context.Context, *connect.Request[v1.WorkRequest]) (*connect.Response[v1.WorkResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.workplace.v1.WorkplaceService.Work is not implemented"))
}

func (UnimplementedWorkplaceServiceHandler) EndShift(context.Context, *connect.Request[v1.EndShiftRequest]) (*connect.Response[v1.EndShiftResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.workplace.v1.WorkplaceService.EndShift is not implemented"))
}

func (UnimplementedWorkplaceServiceHandler) Buy(context.Context, *connect.Request[v1.BuyRequest]) (*connect.Response[v1.BuyResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.workplace.v1.WorkplaceService.Buy is not implemented"))
}
