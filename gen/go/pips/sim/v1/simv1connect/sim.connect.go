// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: pips/sim/v1/sim.proto

package simv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/teceer/pipsim/gen/go/pips/sim/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SimServiceName is the fully-qualified name of the SimService service.
	SimServiceName = "pips.sim.v1.SimService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SimServiceStepProcedure is the fully-qualified name of the SimService's Step RPC.
	SimServiceStepProcedure = "/pips.sim.v1.SimService/Step"
	// SimServiceSnapshotProcedure is the fully-qualified name of the SimService's Snapshot RPC.
	SimServiceSnapshotProcedure = "/pips.sim.v1.SimService/Snapshot"
	// SimServiceWatchDeltasProcedure is the fully-qualified name of the SimService's WatchDeltas RPC.
	SimServiceWatchDeltasProcedure = "/pips.sim.v1.SimService/WatchDeltas"
	// SimServiceSubmitIntentProcedure is the fully-qualified name of the SimService's SubmitIntent RPC.
	SimServiceSubmitIntentProcedure = "/pips.sim.v1.SimService/SubmitIntent"
)

// SimServiceClient is a client for the pips.sim.v1.SimService service.
type SimServiceClient interface {
	// Advances the world by exactly one tick. Called by the tick driver, never
	// by clients. Returns the events produced, which the gateway forwards to Kafka.
	Step(context.Context, *connect.Request[v1.StepRequest]) (*connect.Response[v1.StepResponse], error)
	// Full world snapshot. Expensive — used on client join and for replay
	// verification, not in the render loop.
	Snapshot(context.Context, *connect.Request[v1.SnapshotRequest]) (*connect.Response[v1.SnapshotResponse], error)
	// Streams deltas as they are produced. This is what the render path uses.
	WatchDeltas(context.Context, *connect.Request[v1.WatchDeltasRequest]) (*connect.ServerStreamForClient[v1.WatchDeltasResponse], error)
	// Queues an intent for the next tick. Never applied immediately — applying
	// mid-tick would break determinism.
	SubmitIntent(context.Context, *connect.Request[v1.SubmitIntentRequest]) (*connect.Response[v1.SubmitIntentResponse], error)
}

// NewSimServiceClient constructs a client for the pips.sim.v1.SimService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSimServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SimServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	simServiceMethods := v1.File_pips_sim_v1_sim_proto.Services().ByName("SimService").Methods()
	return &simServiceClient{
		step: connect.NewClient[v1.StepRequest, v1.StepResponse](
			httpClient,
			baseURL+SimServiceStepProcedure,
			connect.WithSchema(simServiceMethods.ByName("Step")),
			connect.WithClientOptions(opts...),
		),
		snapshot: connect.NewClient[v1.SnapshotRequest, v1.SnapshotResponse](
			httpClient,
			baseURL+SimServiceSnapshotProcedure,
			connect.WithSchema(simServiceMethods.ByName("Snapshot")),
			connect.WithClientOptions(opts...),
		),
		watchDeltas: connect.NewClient[v1.WatchDeltasRequest, v1.WatchDeltasResponse](
			httpClient,
			baseURL+SimServiceWatchDeltasProcedure,
			connect.WithSchema(simServiceMethods.ByName("WatchDeltas")),
			connect.WithClientOptions(opts...),
		),
		submitIntent: connect.NewClient[v1.SubmitIntentRequest, v1.SubmitIntentResponse](
			httpClient,
			baseURL+SimServiceSubmitIntentProcedure,
			connect.WithSchema(simServiceMethods.ByName("SubmitIntent")),
			connect.WithClientOptions(opts...),
		),
	}
}

// simServiceClient implements SimServiceClient.
type simServiceClient struct {
	step         *connect.Client[v1.StepRequest, v1.StepResponse]
	snapshot     *connect.Client[v1.SnapshotRequest, v1.SnapshotResponse]
	watchDeltas  *connect.Client[v1.WatchDeltasRequest, v1.WatchDeltasResponse]
	submitIntent *connect.Client[v1.SubmitIntentRequest, v1.SubmitIntentResponse]
}

// Step calls pips.sim.v1.SimService.Step.
func (c *simServiceClient) Step(ctx context.Context, req *connect.Request[v1.StepRequest]) (*connect.Response[v1.StepResponse], error) {
	return c.step.CallUnary(ctx, req)
}

// Snapshot calls pips.sim.v1.SimService.Snapshot.
func (c *simServiceClient) Snapshot(ctx context.Context, req *connect.Request[v1.SnapshotRequest]) (*connect.Response[v1.SnapshotResponse], error) {
	return c.snapshot.CallUnary(ctx, req)
}

// WatchDeltas calls pips.sim.v1.SimService.WatchDeltas.
func (c *simServiceClient) WatchDeltas(ctx context.Context, req *connect.Request[v1.WatchDeltasRequest]) (*connect.ServerStreamForClient[v1.WatchDeltasResponse], error) {
	return c.watchDeltas.CallServerStream(ctx, req)
}

// SubmitIntent calls pips.sim.v1.SimService.SubmitIntent.
func (c *simServiceClient) SubmitIntent(ctx context.Context, req *connect.Request[v1.SubmitIntentRequest]) (*connect.Response[v1.SubmitIntentResponse], error) {
	return c.submitIntent.CallUnary(ctx, req)
}

// SimServiceHandler is an implementation of the pips.sim.v1.SimService service.
type SimServiceHandler interface {
	// Advances the world by exactly one tick. Called by the tick driver, never
	// by clients. Returns the events produced, which the gateway forwards to Kafka.
	Step(context.Context, *connect.Request[v1.StepRequest]) (*connect.Response[v1.StepResponse], error)
	// Full world snapshot. Expensive — used on client join and for replay
	// verification, not in the render loop.
	Snapshot(context.Context, *connect.Request[v1.SnapshotRequest]) (*connect.Response[v1.SnapshotResponse], error)
	// Streams deltas as they are produced. This is what the render path uses.
	WatchDeltas(context.Context, *connect.Request[v1.WatchDeltasRequest], *connect.ServerStream[v1.WatchDeltasResponse]) error
	// Queues an intent for the next tick. Never applied immediately — applying
	// mid-tick would break determinism.
	SubmitIntent(context.Context, *connect.Request[v1.SubmitIntentRequest]) (*connect.Response[v1.SubmitIntentResponse], error)
}

// NewSimServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSimServiceHandler(svc SimServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	simServiceMethods := v1.File_pips_sim_v1_sim_proto.Services().ByName("SimService").Methods()
	simServiceStepHandler := connect.NewUnaryHandler(
		SimServiceStepProcedure,
		svc.Step,
		connect.WithSchema(simServiceMethods.ByName("Step")),
		connect.WithHandlerOptions(opts...),
	)
	simServiceSnapshotHandler := connect.NewUnaryHandler(
		SimServiceSnapshotProcedure,
		svc.Snapshot,
		connect.WithSchema(simServiceMethods.ByName("Snapshot")),
		connect.WithHandlerOptions(opts...),
	)
	simServiceWatchDeltasHandler := connect.NewServerStreamHandler(
		SimServiceWatchDeltasProcedure,
		svc.WatchDeltas,
		connect.WithSchema(simServiceMethods.ByName("WatchDeltas")),
		connect.WithHandlerOptions(opts...),
	)
	simServiceSubmitIntentHandler := connect.NewUnaryHandler(
		SimServiceSubmitIntentProcedure,
		svc.SubmitIntent,
		connect.WithSchema(simServiceMethods.ByName("SubmitIntent")),
		connect.WithHandlerOptions(opts...),
	)
	return "/pips.sim.v1.SimService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SimServiceStepProcedure:
			simServiceStepHandler.ServeHTTP(w, r)
		case SimServiceSnapshotProcedure:
			simServiceSnapshotHandler.ServeHTTP(w, r)
		case SimServiceWatchDeltasProcedure:
			simServiceWatchDeltasHandler.ServeHTTP(w, r)
		case SimServiceSubmitIntentProcedure:
			simServiceSubmitIntentHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSimServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSimServiceHandler struct{}

func (UnimplementedSimServiceHandler) Step(context.Context, *connect.Request[v1.StepRequest]) (*connect.Response[v1.StepResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.sim.v1.SimService.Step is not implemented"))
}

func (UnimplementedSimServiceHandler) Snapshot(context.Context, *connect.Request[v1.SnapshotRequest]) (*connect.Response[v1.SnapshotResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.sim.v1.SimService.Snapshot is not implemented"))
}

func (UnimplementedSimServiceHandler) WatchDeltas(context.Context, *connect.Request[v1.WatchDeltasRequest], *connect.ServerStream[v1.WatchDeltasResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("pips.sim.v1.SimService.WatchDeltas is not implemented"))
}

func (UnimplementedSimServiceHandler) SubmitIntent(context.Context, *connect.Request[v1.SubmitIntentRequest]) (*connect.Response[v1.SubmitIntentResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.sim.v1.SimService.SubmitIntent is not implemented"))
}
