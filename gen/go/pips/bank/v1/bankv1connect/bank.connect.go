// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: pips/bank/v1/bank.proto

package bankv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/teceer/pipsim/gen/go/pips/bank/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// BankServiceName is the fully-qualified name of the BankService service.
	BankServiceName = "pips.bank.v1.BankService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// BankServiceOpenAccountProcedure is the fully-qualified name of the BankService's OpenAccount RPC.
	BankServiceOpenAccountProcedure = "/pips.bank.v1.BankService/OpenAccount"
	// BankServiceTransferProcedure is the fully-qualified name of the BankService's Transfer RPC.
	BankServiceTransferProcedure = "/pips.bank.v1.BankService/Transfer"
	// BankServiceBatchTransferProcedure is the fully-qualified name of the BankService's BatchTransfer
	// RPC.
	BankServiceBatchTransferProcedure = "/pips.bank.v1.BankService/BatchTransfer"
	// BankServiceGetBalanceProcedure is the fully-qualified name of the BankService's GetBalance RPC.
	BankServiceGetBalanceProcedure = "/pips.bank.v1.BankService/GetBalance"
)

// BankServiceClient is a client for the pips.bank.v1.BankService service.
type BankServiceClient interface {
	// Opens an account for a holder that doesn't have one yet. Idempotent:
	// opening an account that already exists returns it unchanged.
	OpenAccount(context.Context, *connect.Request[v1.OpenAccountRequest]) (*connect.Response[v1.OpenAccountResponse], error)
	// Moves money from one account to another. (payer, payee, tick, kind) is
	// the idempotency key — a retried call with the same key returns the
	// original result rather than moving money twice.
	Transfer(context.Context, *connect.Request[v1.TransferRequest]) (*connect.Response[v1.TransferResponse], error)
	// One payer, many payees, one call. This is payroll: a workplace pays
	// every pip on shift in a single round trip instead of one Transfer per
	// pip, which is what keeps a tick's payroll inside the tick budget.
	BatchTransfer(context.Context, *connect.Request[v1.BatchTransferRequest]) (*connect.Response[v1.BatchTransferResponse], error)
	GetBalance(context.Context, *connect.Request[v1.GetBalanceRequest]) (*connect.Response[v1.GetBalanceResponse], error)
}

// NewBankServiceClient constructs a client for the pips.bank.v1.BankService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewBankServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) BankServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	bankServiceMethods := v1.File_pips_bank_v1_bank_proto.Services().ByName("BankService").Methods()
	return &bankServiceClient{
		openAccount: connect.NewClient[v1.OpenAccountRequest, v1.OpenAccountResponse](
			httpClient,
			baseURL+BankServiceOpenAccountProcedure,
			connect.WithSchema(bankServiceMethods.ByName("OpenAccount")),
			connect.WithClientOptions(opts...),
		),
		transfer: connect.NewClient[v1.TransferRequest, v1.TransferResponse](
			httpClient,
			baseURL+BankServiceTransferProcedure,
			connect.WithSchema(bankServiceMethods.ByName("Transfer")),
			connect.WithClientOptions(opts...),
		),
		batchTransfer: connect.NewClient[v1.BatchTransferRequest, v1.BatchTransferResponse](
			httpClient,
			baseURL+BankServiceBatchTransferProcedure,
			connect.WithSchema(bankServiceMethods.ByName("BatchTransfer")),
			connect.WithClientOptions(opts...),
		),
		getBalance: connect.NewClient[v1.GetBalanceRequest, v1.GetBalanceResponse](
			httpClient,
			baseURL+BankServiceGetBalanceProcedure,
			connect.WithSchema(bankServiceMethods.ByName("GetBalance")),
			connect.WithClientOptions(opts...),
		),
	}
}

// bankServiceClient implements BankServiceClient.
type bankServiceClient struct {
	openAccount   *connect.Client[v1.OpenAccountRequest, v1.OpenAccountResponse]
	transfer      *connect.Client[v1.TransferRequest, v1.TransferResponse]
	batchTransfer *connect.Client[v1.BatchTransferRequest, v1.BatchTransferResponse]
	getBalance    *connect.Client[v1.GetBalanceRequest, v1.GetBalanceResponse]
}

// OpenAccount calls pips.bank.v1.BankService.OpenAccount.
func (c *bankServiceClient) OpenAccount(ctx context.Context, req *connect.Request[v1.OpenAccountRequest]) (*connect.Response[v1.OpenAccountResponse], error) {
	return c.openAccount.CallUnary(ctx, req)
}

// Transfer calls pips.bank.v1.BankService.Transfer.
func (c *bankServiceClient) Transfer(ctx context.Context, req *connect.Request[v1.TransferRequest]) (*connect.Response[v1.TransferResponse], error) {
	return c.transfer.CallUnary(ctx, req)
}

// BatchTransfer calls pips.bank.v1.BankService.BatchTransfer.
func (c *bankServiceClient) BatchTransfer(ctx context.Context, req *connect.Request[v1.BatchTransferRequest]) (*connect.Response[v1.BatchTransferResponse], error) {
	return c.batchTransfer.CallUnary(ctx, req)
}

// GetBalance calls pips.bank.v1.BankService.GetBalance.
func (c *bankServiceClient) GetBalance(ctx context.Context, req *connect.Request[v1.GetBalanceRequest]) (*connect.Response[v1.GetBalanceResponse], error) {
	return c.getBalance.CallUnary(ctx, req)
}

// BankServiceHandler is an implementation of the pips.bank.v1.BankService service.
type BankServiceHandler interface {
	// Opens an account for a holder that doesn't have one yet. Idempotent:
	// opening an account that already exists returns it unchanged.
	OpenAccount(context.Context, *connect.Request[v1.OpenAccountRequest]) (*connect.Response[v1.OpenAccountResponse], error)
	// Moves money from one account to another. (payer, payee, tick, kind) is
	// the idempotency key — a retried call with the same key returns the
	// original result rather than moving money twice.
	Transfer(context.Context, *connect.Request[v1.TransferRequest]) (*connect.Response[v1.TransferResponse], error)
	// One payer, many payees, one call. This is payroll: a workplace pays
	// every pip on shift in a single round trip instead of one Transfer per
	// pip, which is what keeps a tick's payroll inside the tick budget.
	BatchTransfer(context.Context, *connect.Request[v1.BatchTransferRequest]) (*connect.Response[v1.BatchTransferResponse], error)
	GetBalance(context.Context, *connect.Request[v1.GetBalanceRequest]) (*connect.Response[v1.GetBalanceResponse], error)
}

// NewBankServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewBankServiceHandler(svc BankServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	bankServiceMethods := v1.File_pips_bank_v1_bank_proto.Services().ByName("BankService").Methods()
	bankServiceOpenAccountHandler := connect.NewUnaryHandler(
		BankServiceOpenAccountProcedure,
		svc.OpenAccount,
		connect.WithSchema(bankServiceMethods.ByName("OpenAccount")),
		connect.WithHandlerOptions(opts...),
	)
	bankServiceTransferHandler := connect.NewUnaryHandler(
		BankServiceTransferProcedure,
		svc.Transfer,
		connect.WithSchema(bankServiceMethods.ByName("Transfer")),
		connect.WithHandlerOptions(opts...),
	)
	bankServiceBatchTransferHandler := connect.NewUnaryHandler(
		BankServiceBatchTransferProcedure,
		svc.BatchTransfer,
		connect.WithSchema(bankServiceMethods.ByName("BatchTransfer")),
		connect.WithHandlerOptions(opts...),
	)
	bankServiceGetBalanceHandler := connect.NewUnaryHandler(
		BankServiceGetBalanceProcedure,
		svc.GetBalance,
		connect.WithSchema(bankServiceMethods.ByName("GetBalance")),
		connect.WithHandlerOptions(opts...),
	)
	return "/pips.bank.v1.BankService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case BankServiceOpenAccountProcedure:
			bankServiceOpenAccountHandler.ServeHTTP(w, r)
		case BankServiceTransferProcedure:
			bankServiceTransferHandler.ServeHTTP(w, r)
		case BankServiceBatchTransferProcedure:
			bankServiceBatchTransferHandler.ServeHTTP(w, r)
		case BankServiceGetBalanceProcedure:
			bankServiceGetBalanceHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedBankServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedBankServiceHandler struct{}

func (UnimplementedBankServiceHandler) OpenAccount(context.Context, *connect.Request[v1.OpenAccountRequest]) (*connect.Response[v1.OpenAccountResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.bank.v1.BankService.OpenAccount is not implemented"))
}

func (UnimplementedBankServiceHandler) Transfer(context.Context, *connect.Request[v1.TransferRequest]) (*connect.Response[v1.TransferResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.bank.v1.BankService.Transfer is not implemented"))
}

func (UnimplementedBankServiceHandler) BatchTransfer(context.Context, *connect.Request[v1.BatchTransferRequest]) (*connect.Response[v1.BatchTransferResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.bank.v1.BankService.BatchTransfer is not implemented"))
}

func (UnimplementedBankServiceHandler) GetBalance(context.Context, *connect.Request[v1.GetBalanceRequest]) (*connect.Response[v1.GetBalanceResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("pips.bank.v1.BankService.GetBalance is not implemented"))
}
